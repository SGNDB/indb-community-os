# Graatek Translations

Graatek translations still live in the main `messages/` files for now.

When module-scoped translation loading is introduced, move the `Fadla`, `Meta.fadla`, `Admin.graatekPage`, notification labels, empty states, and related profile/admin labels here while keeping compatibility notes for existing `fadla` message keys.
