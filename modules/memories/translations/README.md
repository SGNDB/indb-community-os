# Memories Translations

Memories translations still live in the main `messages/` files for now.

When module-scoped translation loading is introduced, move the `Memory`, `Meta.memory`, `Meta.memorySubmit`, `Timeline`, and related empty-state/admin labels here and keep compatibility notes for existing message keys.
